name: Build ZMK Sofle (Dongle)
on:
  push:
    paths:
      - "config/keys.keymap"
      - "dongle/**"
    branches: ["main"]
  workflow_dispatch:
jobs:
  build:
    uses: ./.github/workflows/build-user-config.yml
    with:
      config_path: dongle/config
      build_matrix_path: dongle/build.yaml
      archive_name: firmware-dongle
