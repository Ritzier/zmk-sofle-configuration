name: Build ZMK Sofle (Dongle)
on:
  push:
    paths:
      - "config/eyelash_sofle.keymap"
      - "dongle/**"
    branches: ["main"]
  workflow_dispatch:
jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3.0
    with:
      config-path: dongle/config
      build-matrix-path: dongle/build.yaml
      archive-name: firmware-dongle
